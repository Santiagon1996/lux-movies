import React from "react";
import { Link } from "react-router-dom";

const NavButtonLux: React.FC = () => (
    <Link to="/" className="header-title">
        LuxMovies
    </Link>
);

export default NavButtonLux;
